# Полный отчёт о багах - DataCheck Pro

**Дата аудита:** 4 февраля 2026  
**Версия:** 2.3.0

---

## 🔴 КРИТИЧЕСКИЕ БАГИ (Блокируют функциональность)

### 1. Email checkSingle использует неправильный permission
**Файл:** `server/routers.ts`, строка ~1036  
**Проблема:** Проверка одиночного email использует permission `hlr.single` вместо `email.single`
```typescript
// НЕПРАВИЛЬНО:
const hasPermission = await userHasPermission(ctx.user.id, "hlr.single");
// ДОЛЖНО БЫТЬ:
const hasPermission = await userHasPermission(ctx.user.id, "email.single");
```
**Влияние:** Пользователи с правами только на email проверку не смогут делать одиночные проверки email

### 2. Email listBatches использует неправильный permission
**Файл:** `server/routers.ts`, строка ~1288  
**Проблема:** Просмотр списка email батчей использует permission `hlr.history` вместо `email.history`
```typescript
// НЕПРАВИЛЬНО:
const hasPermission = await userHasPermission(ctx.user.id, "hlr.history");
// ДОЛЖНО БЫТЬ:
const hasPermission = await userHasPermission(ctx.user.id, "email.history");
```
**Влияние:** Пользователи с правами только на email историю не смогут видеть свои email батчи

### 3. Email deleteBatch использует неправильный permission
**Файл:** `server/routers.ts`, строка ~1324  
**Проблема:** Удаление email батча использует permission `hlr.delete` вместо `email.delete`
```typescript
// НЕПРАВИЛЬНО:
const hasPermission = await userHasPermission(ctx.user.id, "hlr.delete");
// ДОЛЖНО БЫТЬ:
const hasPermission = await userHasPermission(ctx.user.id, "email.delete");
```
**Влияние:** Пользователи с правами только на удаление email батчей не смогут их удалять

### 4. Email exportXlsx использует неправильный permission
**Файл:** `server/routers.ts`, строка ~1343  
**Проблема:** Экспорт email результатов использует permission `hlr.export` вместо `email.export`
```typescript
// НЕПРАВИЛЬНО:
const hasPermission = await userHasPermission(ctx.user.id, "hlr.export");
// ДОЛЖНО БЫТЬ:
const hasPermission = await userHasPermission(ctx.user.id, "email.export");
```
**Влияние:** Пользователи с правами только на экспорт email не смогут экспортировать результаты

### 5. Email exportWithFields использует неправильный permission
**Файл:** `server/routers.ts`, строка ~1400  
**Проблема:** Экспорт email с выбором полей использует permission `hlr.export` вместо `email.export`
```typescript
// НЕПРАВИЛЬНО:
const hasPermission = await userHasPermission(ctx.user.id, "hlr.export");
// ДОЛЖНО БЫТЬ:
const hasPermission = await userHasPermission(ctx.user.id, "email.export");
```
**Влияние:** Пользователи с правами только на экспорт email не смогут экспортировать с выбором полей

---

## 🟠 СЕРЬЁЗНЫЕ БАГИ (Вызывают некорректное поведение)

### 6. HLR resumeBatch не обрабатывает оставшиеся номера
**Файл:** `server/routers.ts`, строки ~2480-2538  
**Проблема:** Функция `resumeBatch` только помечает батч как завершённый, но НЕ обрабатывает оставшиеся номера. Она возвращает сообщение о том, что номера не были обработаны, вместо их обработки.
```typescript
// Текущее поведение:
// Mark batch as completed with partial results
await updateHlrBatch(input.batchId, {
  status: "completed",
  completedAt: new Date(),
});
return { 
  message: `Batch marked as completed. ${alreadyChecked} numbers were successfully checked. ${remaining} numbers were not processed due to interruption.`,
};
```
**Влияние:** Resume функциональность для HLR не работает - батч просто помечается как завершённый без обработки оставшихся номеров

### 7. HLR resumeBatchWithNumbers требует передачи номеров от клиента
**Файл:** `server/routers.ts`, строки ~2541-2677  
**Проблема:** Функция `resumeBatchWithNumbers` требует, чтобы клиент передал список номеров, но в UI нет механизма для этого. Пользователь должен повторно загрузить файл с номерами.
**Влияние:** Неудобный UX - пользователь должен помнить/иметь исходный файл для возобновления

---

## 🟡 НЕЗНАЧИТЕЛЬНЫЕ БАГИ (UI/UX проблемы)

### 8. Отсутствует индикация прогресса при batch обработке
**Файл:** `client/src/pages/Home.tsx`  
**Проблема:** Progress bar показывает 0% во время обработки, так как нет механизма обновления прогресса в реальном времени (batch обрабатывается синхронно на сервере)
**Влияние:** Пользователь не видит реальный прогресс обработки

### 9. Нет валидации email на клиенте перед отправкой
**Файл:** `client/src/pages/EmailValidator.tsx`  
**Проблема:** Клиент не показывает, сколько email адресов валидны до отправки на сервер
**Влияние:** Пользователь может не знать, что часть адресов будет отфильтрована

---

## ✅ УЖЕ ИСПРАВЛЕНО (в предыдущем коммите)

1. ✅ Email startBatch - permission исправлен на `email.batch`
2. ✅ Email resumeBatch - permission исправлен на `email.batch`
3. ✅ HLR startBatch - добавлена задержка 150ms между API вызовами
4. ✅ HLR resumeBatchWithNumbers - добавлена задержка 150ms между API вызовами
5. ✅ Email regex - обновлён на более permissive паттерн

---

## 📋 РЕКОМЕНДУЕМЫЙ ПОРЯДОК ИСПРАВЛЕНИЙ

### Приоритет 1 - Критические (исправить немедленно):
1. Email checkSingle permission → `email.single`
2. Email listBatches permission → `email.history`
3. Email deleteBatch permission → `email.delete`
4. Email exportXlsx permission → `email.export`
5. Email exportWithFields permission → `email.export`

### Приоритет 2 - Серьёзные (исправить в следующем релизе):
6. HLR resumeBatch - переработать логику для реальной обработки оставшихся номеров
7. Сохранять исходный список номеров в батче для возможности resume без повторной загрузки

### Приоритет 3 - Улучшения (планировать на будущее):
8. Добавить WebSocket/SSE для real-time прогресса
9. Добавить предварительную валидацию email на клиенте

---

## 📊 СТАТИСТИКА

| Категория | Количество |
|-----------|------------|
| Критические баги | 5 |
| Серьёзные баги | 2 |
| Незначительные баги | 2 |
| **Всего** | **9** |

---

**Примечание:** Все критические баги связаны с неправильными проверками permissions в email router. Это системная ошибка - при создании email функциональности были скопированы проверки из HLR router без обновления permission strings.
