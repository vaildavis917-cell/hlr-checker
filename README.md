# HLR Bulk Checker

<p align="center">
  <img src="https://img.shields.io/badge/version-1.6.0-blue.svg" alt="Version">
  <img src="https://img.shields.io/badge/license-MIT-green.svg" alt="License">
  <img src="https://img.shields.io/badge/node-%3E%3D18.0.0-brightgreen.svg" alt="Node">
  <img src="https://img.shields.io/badge/typescript-5.x-blue.svg" alt="TypeScript">
</p>

Профессиональный сервис массовой проверки телефонных номеров через HLR (Home Location Register) lookup. Позволяет проверять валидность номеров, определять оператора, статус активности и качество номера.

## 📋 Содержание

- [Возможности](#-возможности)
- [Технологии](#-технологии)
- [Требования](#-требования)
- [Быстрый старт](#-быстрый-старт)
- [Установка](#-установка)
- [Конфигурация](#-конфигурация)
- [Запуск](#-запуск)
- [GSM коды](#-gsm-коды)
- [API](#-api)
- [Структура проекта](#-структура-проекта)
- [Разработка](#-разработка)
- [Развёртывание](#-развёртывание)
- [FAQ](#-faq)
- [Лицензия](#-лицензия)

## ✨ Возможности

### Основные функции
- **Массовая проверка** — до 2000+ номеров за один запрос
- **Загрузка файлов** — поддержка CSV и TXT форматов
- **Экспорт результатов** — CSV и Excel (XLSX)
- **История проверок** — сохранение всех результатов с возможностью повторного просмотра

### Аналитика
- **Health Score** — оценка качества номера от 0 до 100
- **GSM статусы** — детальная информация о причине невалидности
- **Обнаружение дубликатов** — автоматическое выявление повторяющихся номеров
- **Статистика** — графики и отчёты по проверкам

### Оптимизация
- **Кэширование** — результаты сохраняются на 24 часа
- **Возобновление** — прерванные проверки можно продолжить
- **Прогресс в реальном времени** — отслеживание процесса проверки

### Безопасность
- **Авторизация** — собственная система логин/пароль
- **Блокировка** — защита от брутфорса (5 попыток)
- **Лимиты** — ограничение количества проверок на пользователя
- **Роли** — разделение на администраторов и пользователей

### Интерфейс
- **Многоязычность** — русский, украинский, английский
- **Тёмная тема** — современный дизайн
- **Адаптивность** — работает на мобильных устройствах
- **Help Center** — встроенная справка

## 🛠 Технологии

| Категория | Технология |
|-----------|------------|
| Frontend | React 19, TypeScript, Tailwind CSS 4 |
| Backend | Node.js, Express 4, tRPC 11 |
| База данных | MySQL 8 / TiDB / PlanetScale |
| ORM | Drizzle ORM |
| API | Seven.io HLR Lookup |
| Тестирование | Vitest |
| Сборка | Vite |

## 📦 Требования

- **Node.js** >= 18.0.0
- **pnpm** >= 8.0.0 (рекомендуется) или npm
- **MySQL** >= 8.0 или совместимая БД (TiDB, PlanetScale)
- **Seven.io API ключ** — [получить здесь](https://www.seven.io/)

## 🚀 Быстрый старт

```bash
# Клонирование
git clone https://github.com/your-username/hlr-checker.git
cd hlr-checker

# Установка зависимостей
pnpm install

# Настройка окружения
cp .env.example .env
# Отредактируйте .env файл

# Миграции БД
pnpm db:push

# Запуск
pnpm dev
```

Приложение будет доступно по адресу `http://localhost:3000`

## 📥 Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/your-username/hlr-checker.git
cd hlr-checker
```

### 2. Установка зависимостей

С использованием pnpm (рекомендуется):
```bash
pnpm install
```

С использованием npm:
```bash
npm install
```

### 3. Настройка базы данных

Создайте базу данных MySQL:

```sql
CREATE DATABASE hlr_checker CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'hlr_user'@'localhost' IDENTIFIED BY 'your_password';
GRANT ALL PRIVILEGES ON hlr_checker.* TO 'hlr_user'@'localhost';
FLUSH PRIVILEGES;
```

## ⚙️ Конфигурация

Создайте файл `.env` в корне проекта:

```env
# ============================================
# БАЗА ДАННЫХ
# ============================================
# Формат: mysql://user:password@host:port/database
DATABASE_URL=mysql://hlr_user:your_password@localhost:3306/hlr_checker

# ============================================
# SEVEN.IO API
# ============================================
# Получите ключ на https://www.seven.io/
SEVEN_IO_API_KEY=your_seven_io_api_key

# ============================================
# БЕЗОПАСНОСТЬ
# ============================================
# Секретный ключ для JWT токенов (минимум 32 символа)
# Сгенерируйте: openssl rand -hex 32
JWT_SECRET=your_very_long_and_secure_secret_key_here

# ============================================
# ПРИЛОЖЕНИЕ
# ============================================
# Название приложения
VITE_APP_TITLE=HLR Bulk Checker

# Порт сервера (опционально, по умолчанию 3000)
PORT=3000

# Режим работы: development | production
NODE_ENV=production
```

### Описание переменных

| Переменная | Обязательная | Описание |
|------------|--------------|----------|
| `DATABASE_URL` | Да | Строка подключения к MySQL |
| `SEVEN_IO_API_KEY` | Да | API ключ Seven.io для HLR запросов |
| `JWT_SECRET` | Да | Секрет для подписи JWT токенов |
| `VITE_APP_TITLE` | Нет | Название приложения в интерфейсе |
| `PORT` | Нет | Порт сервера (по умолчанию 3000) |
| `NODE_ENV` | Нет | Режим работы приложения |

## ▶️ Запуск

### Режим разработки

```bash
pnpm dev
```

### Production сборка

```bash
# Сборка
pnpm build

# Запуск
pnpm start
```

### Миграции базы данных

```bash
# Применить миграции
pnpm db:push

# Сгенерировать новую миграцию (после изменения схемы)
pnpm db:generate
```

## 📊 GSM коды

При проверке номера API возвращает GSM код, указывающий на статус номера:

| Код | Статус | Описание | Рекомендация |
|-----|--------|----------|--------------|
| 0 | OK (Delivered) | Номер активен, сообщение доставлено | Номер валидный, можно использовать |
| 1 | Bad Number | Unknown Subscriber — номер не существует | Удалить из базы |
| 5 | Bad Number | Unidentified Subscriber — неопознанный абонент | Удалить из базы |
| 6 | Absent | Absent Subscriber — телефон выключен | Перепроверить позже |
| 8 | Roaming | Абонент в роуминге | Номер активен, но в роуминге |
| 9 | Blocked | Illegal Subscriber — номер заблокирован | Удалить из базы |
| 10 | No Service | Network out of order — сеть недоступна | Перепроверить позже |
| 11 | No Service | Network out of order — сеть недоступна | Перепроверить позже |
| 12 | Blocked | Illegal Equipment — устройство заблокировано | Удалить из базы |
| 13 | Barred | Call Barred — вызовы заблокированы | Удалить из базы |
| 27 | Absent | Absent Subscriber — абонент недоступен | Перепроверить позже |
| 31 | Error | System Failure — системная ошибка | Перепроверить позже |
| 32 | Error | Data Missing — данные отсутствуют | Перепроверить позже |

**Автоматическая классификация:** Номера с кодами 1, 5, 9, 12 автоматически помечаются как `invalid`.

## 🔌 API

Приложение использует tRPC для API. Основные эндпоинты:

### Аутентификация
- `auth.login` — вход в систему
- `auth.logout` — выход
- `auth.me` — текущий пользователь

### HLR проверка
- `hlr.checkSingle` — проверка одного номера
- `hlr.checkBatch` — массовая проверка
- `hlr.resumeBatch` — возобновление проверки
- `hlr.getBatches` — список проверок
- `hlr.getResults` — результаты проверки
- `hlr.exportResults` — экспорт в CSV/Excel

### Администрирование
- `admin.getUsers` — список пользователей
- `admin.createUser` — создание пользователя
- `admin.updateUser` — обновление пользователя
- `admin.deleteUser` — удаление пользователя

## 📁 Структура проекта

```
hlr-checker/
├── client/                 # Frontend (React)
│   ├── public/            # Статические файлы
│   └── src/
│       ├── components/    # React компоненты
│       │   └── ui/       # UI компоненты (shadcn/ui)
│       ├── contexts/      # React контексты
│       ├── hooks/         # Кастомные хуки
│       ├── lib/           # Утилиты
│       ├── pages/         # Страницы приложения
│       ├── App.tsx        # Главный компонент
│       └── main.tsx       # Точка входа
├── server/                 # Backend (Express + tRPC)
│   ├── db.ts              # Функции работы с БД
│   ├── routers.ts         # tRPC роутеры
│   ├── auth.ts            # Логика авторизации
│   ├── phoneUtils.ts      # Утилиты для номеров
│   └── *.test.ts          # Тесты
├── drizzle/                # База данных
│   ├── schema.ts          # Схема таблиц
│   ├── relations.ts       # Связи между таблицами
│   └── *.sql              # Миграции
├── shared/                 # Общий код
│   ├── types.ts           # TypeScript типы
│   └── const.ts           # Константы
├── package.json
├── tsconfig.json
├── vite.config.ts
└── vitest.config.ts
```

## 💻 Разработка

### Запуск тестов

```bash
# Все тесты
pnpm test

# С покрытием
pnpm test:coverage

# В watch режиме
pnpm test:watch
```

### Линтинг

```bash
pnpm lint
```

### Типы

```bash
pnpm typecheck
```

### Работа с базой данных

```bash
# Открыть Drizzle Studio (GUI для БД)
pnpm db:studio

# Сгенерировать миграцию
pnpm db:generate

# Применить миграции
pnpm db:push
```

## 🌐 Развёртывание

Подробная инструкция по развёртыванию находится в файле [DEPLOY.md](./DEPLOY.md).

### Быстрое развёртывание на VPS

```bash
# 1. Подключитесь к серверу
ssh user@your-server

# 2. Установите Node.js 18+
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# 3. Установите pnpm
npm install -g pnpm

# 4. Клонируйте и настройте проект
git clone https://github.com/your-username/hlr-checker.git
cd hlr-checker
pnpm install
cp .env.example .env
nano .env  # Настройте переменные

# 5. Соберите и запустите
pnpm build
pnpm start
```

### Docker

```bash
docker build -t hlr-checker .
docker run -p 3000:3000 --env-file .env hlr-checker
```

## ❓ FAQ

**Q: Как получить API ключ Seven.io?**
A: Зарегистрируйтесь на [seven.io](https://www.seven.io/), перейдите в настройки аккаунта и создайте API ключ.

**Q: Сколько стоит проверка одного номера?**
A: Стоимость зависит от тарифа Seven.io, обычно около €0.005-0.01 за номер.

**Q: Почему некоторые номера показывают Unknown?**
A: Это может быть связано с временной недоступностью оператора или нестандартным ответом API.

**Q: Как добавить нового пользователя?**
A: Войдите как администратор, перейдите в раздел "Пользователи" и нажмите "Добавить".

**Q: Как сбросить пароль?**
A: Администратор может сбросить пароль через панель управления пользователями.

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. Подробности в файле [LICENSE](./LICENSE).

## 👨‍💻 Автор

Разработано с ❤️

---

<p align="center">
  <a href="#hlr-bulk-checker">⬆️ Наверх</a>
</p>
